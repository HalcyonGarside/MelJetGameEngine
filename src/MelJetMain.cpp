/* MelJetMain.cpp
 *
 *
 */
//This thing maybe?
#include <windows.h>

/*
 * hInstance - Window Handle
 * nCmdShow - Normal/Minimized/Maximized window that you see in window properties (ok microsoft)
 */
int CALLBACK WinMain(
	HINSTANCE hInstance,
	HINSTANCE hPrevInstance,
	LPSTR lpCmdLine,
	int nCmdShow
)
{
	return(0);
}
